import { UserNamesDisplayPipe } from './user-names-display.pipe';

describe('UserNamesDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new UserNamesDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
